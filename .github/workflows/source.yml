name: News monitor

permissions:
  contents: write

env:
  SCHOLARLY_API_EMAIL: "radim.tolasz@chmi.cz"

on:
  workflow_dispatch:
    inputs:
      date_from:
        description: "Start date in YYYY-MM-DD"
        required: false
      date_to:
        description: "End date in YYYY-MM-DD"
        required: false
  schedule:
    - cron: '10 2,14 * * *'

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check Prague time
        id: time
        shell: bash
        run: |
          H=$(TZ=Europe/Prague date +%H)
          echo "hour=$H" >> "$GITHUB_OUTPUT"
          echo "Hour in Prague: $H"

      - name: Stop outside scheduled hours
        if: steps.time.outputs.hour != '03' && steps.time.outputs.hour != '15' && github.event_name != 'workflow_dispatch'
        shell: bash
        run: |
          echo "Skipping run outside 03:00 and 15:00 Europe/Prague."
          exit 0

      - name: Setup R
        uses: r-lib/actions/setup-r@v2

      - name: Install system libraries
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y libcurl4-openssl-dev libxml2-dev libssl-dev

      - name: Install R packages
        shell: bash
        run: |
          Rscript -e 'install.packages(c(
            "dplyr","purrr","stringr","readr",
            "tibble","xml2","tidyr","rvest"
          ), repos = "https://cloud.r-project.org")'

      - name: Run script
        shell: bash
        run: |
          Rscript source.R "${{ github.event.inputs.date_from }}" "${{ github.event.inputs.date_to }}"

      - name: Show generated files
        shell: bash
        run: |
          echo "Files in workspace:"
          ls -la
          echo ""
          echo "Preview of source.csv:"
          if [ -f source.csv ]; then
            head -n 20 source.csv
          else
            echo "source.csv not found"
          fi
          echo ""
          echo "Preview of news_monitor.log:"
          if [ -f news_monitor.log ]; then
            tail -n 50 news_monitor.log
          else
            echo "news_monitor.log not found"
          fi

      - name: Commit and push results
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add source.csv news_monitor.log source-crossref.csv

          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "Auto-update news monitoring output"

          for i in 1 2 3; do
            echo "Push attempt $i..."
            git pull --rebase origin main || true

            if git push; then
              echo "Push succeeded"
              exit 0
            fi

            echo "Push failed, retrying in 10s..."
            sleep 10
          done

          echo "Push failed after retries"
          exit 1

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: news-monitor-output
          path: |
            source.csv
            news_monitor.log
          if-no-files-found: warn
